[pytest]
DJANGO_SETTINGS_MODULE = backend.settings_test
pythonpath = backend
testpaths = tests
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
markers =
    slow: marks tests as slow
